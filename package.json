{
    "name": "portfolio",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "cp CNAME sitemap.xml dist",
        "preview": "vite preview",
        "deploy": "npm run build && gh-pages -d dist --nojekyll",
        "check": "svelte-check --tsconfig ./jsconfig.json",
        "lint": "prettier --check .",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^7.3.0",
        "gh-pages": "^6.1.1",
        "prettier": "^3.8.1",
        "prettier-plugin-svelte": "^4.1.1",
        "svelte": "^5.57.0",
        "svelte-check": "^4.7.6",
        "vite": "^8.2.2"
    }
}
