name: Deploy

on:
    push:
        branches:
            - master
        tags-ignore:
            - '**'

permissions:
    contents: write

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Check formatting
              run: npm run lint

            - name: Check Svelte
              run: npm run check

            - name: Build page
              run: npm run build

            - name: Publish GitHub Pages
              run: npx gh-pages -d dist -u "github-actions-bot <support+actions@github.com>" --nojekyll
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
